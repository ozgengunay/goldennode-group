#!/bin/bash -e

case "$1" in
  disable)
	sed -i '/127.0.0.1 goldennode.com/d' /etc/hosts 
    ;;
  enable)
 	echo "127.0.0.1 goldennode.com" >> /etc/hosts
    ;;
  status)
    cat /etc/hosts
    ;;
  *)
    echo "Usage: $0 {disable|enable|status}"
    exit 1
esac

